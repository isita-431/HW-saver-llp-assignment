# HW-saver-llp-assignment
Assignment where i used opencv to clean and crop images.
The boxdetect package which is created using openCV is used.

command to install the package:

pip install boxdetect
